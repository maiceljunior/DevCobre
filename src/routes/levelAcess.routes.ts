import { Router } from "express";

const routes=Router();


export const levelAcessRoutes = () =>{
    routes.get("");
    routes.post("");
    // routes.patch("/:id");
    // routes.delete("/:id");
    return routes

};

